import {Stack} from "expo-router";
import React from "react";

export default (props) => {
    return (
        <Stack screenOptions={{
            headerShown: false,
        }}>
        </Stack>
    )
}